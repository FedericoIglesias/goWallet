package servicesAccount

import (
	// "fmt"
	// "goWallet/internal/domain"
)

// func (s *Services) Get(account *domain.Account) (*domain.Account, error) {
// 	account, err = s.Repo.Get(&account.UserID)

// 	if err != nil {
// 		return account, fmt.Errorf("some pass when get account: %w", err)
// 	}
// 	return account, nil
// }
